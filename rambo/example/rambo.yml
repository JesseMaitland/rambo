commands:
  action:
    help: "available options for the rambo action argument"
    choices:
      - create
      - view
  object:
    help: "available options for the object argument"
    choices:
      - project
      - file
      - config
